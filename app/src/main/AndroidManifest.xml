<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/camera_freepik_image_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MT2026GroupA2">
        <activity
            android:name=".Activity6MLK"
            android:exported="false" />
        <activity
            android:name=".ActivityLoadImage"
            android:exported="false" />
        <activity
            android:name=".Activity3OpenCamera"
            android:exported="false"
            android:theme="@style/Theme.MT2026GroupA2" />
        <activity
            android:name=".Activity2"
            android:exported="false" />
        <activity
            android:name=".Activity1Main"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>